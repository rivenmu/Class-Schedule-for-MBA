# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Project runtime / cache
.omo/
.codegraph/
node_modules/
*.log
.playwright-mcp/

# Original course tables - local only, not tracked (all PDFs)
*.pdf
**/*.pdf

# deploy.sh 注入后的上线文件 (本机生成, 不进仓库)
/web/

# Secrets - never commit real values
api/.env

# Temp
*.tmp
*.bak
test-results/

# compose 真值 (deploy.sh 生成, 含数据库密码)
.env
