{
  "name": "mba-schedule-mobile-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile adaptation end-to-end tests for the MBA course schedule HTML app.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "baseline": "node ./scripts/baseline-screenshots.mjs",
    "serve": "python3 -m http.server 8765 --directory ..."
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
